# Project Notes

The task board starts intentionally small. New capabilities should be added
through focused branches and reviewed before they reach `main`.

Task records may include an optional `priority` field. When present, the
command-line output displays it next to the task title.
